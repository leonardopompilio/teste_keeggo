**Projeto Cypress com Cucumber**

Este projeto utiliza Cypress para testes de ponta a ponta e Cucumber para desenvolvimento orientado a comportamento (BDD)

**Pré-requisitos**

Antes de começar, certifique-se de ter atendido aos seguintes requisitos:

Você instalou a versão mais recente do `Node.js`, `Npm`, `Cucumber` e `Cypress`.

**Rodando os testes**

No terminal da sua IDE rode `Npm install` para criar o node_modules.

Após isso, você irá rodar também no terminar o comando:

```
Npx Cypress Open
```

Após abrir o `Cypress` você deverá escolher qual a Feature você irá testar.
